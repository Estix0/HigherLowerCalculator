# Higher / Lower Odds Tracker

A Streamlit app for tracking probabilities in a Higher/Lower card game.  
- Select a custom range or define a full custom deck.
- Track seen cards across suits.
- View real-time odds of drawing higher/lower cards.
- See suit probabilities.
## Requirements

Install dependencies:

```bash
pip install -r requirements.txt
